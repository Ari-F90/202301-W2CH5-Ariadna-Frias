import { add } from "./tools.js";
describe("sdfsd", () => {
  test("sdfsdf", () => {
    const a = 1;
    const b = 1;
    const result = add(a, b);
    const expected = 2;
    expect(result).toBe(expected);
  });
});
